# Shift Note - The Whispering Shelf

## Quick Start

1. Double-click `server.py` (or run `python3 server.py`)
2. Browser opens automatically at `http://localhost:8000`

## Setup

Follow `SETUP.md` to connect your Google Sheet.
